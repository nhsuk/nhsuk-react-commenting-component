import {
  Button,
  Icon,
} from './index';

describe('wagtail package API', () => {
  it('has Button', () => {
    expect(Button).toBeDefined();
  });

  it('has Icon', () => {
    expect(Icon).toBeDefined();
  });
});
